"""eval_tools — ROS-free trajectory evaluation (ATE/RPE) + chart generation.

The evaluation backbone for the milestone 'money charts' (PLAN §6). Reusable by
every dataset/sim milestone that needs to compare a frontend or the fusion core
against ground truth.
"""
